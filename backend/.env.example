# ============================================
# frp-admin 配置文件示例
# 复制此文件为 .env 并根据实际情况修改
# ============================================

# ----- 基础配置 -----

# Web 服务监听端口
FRP_ADMIN_PORT=8080

# SQLite 数据库文件路径
FRP_ADMIN_DB=frp_admin.db

# JWT 密钥（用于登录认证，留空则自动生成）
# 注意：如果不设置，每次重启后之前的登录会话会失效
FRP_ADMIN_SECRET=your-secret-key-here

# ----- frps 相关配置 -----

# frps 二进制文件路径
# 默认：与 frp-admin 同目录下的 frps
FRP_ADMIN_FRPS_PATH=/usr/local/bin/frps

# frps 配置文件路径
# 默认：与 frp-admin 同目录下的 frps.toml
FRP_ADMIN_FRPS_CONFIG=/etc/frp/frps.toml

# frps 管理方式
# - process: 直接管理进程（默认，适合 frp-admin 独立启动 frps 的场景）
# - systemctl: 通过 systemctl 管理（适合 frps 已由 systemd 管理的场景）
FRP_ADMIN_FRPS_MANAGER=process

# systemctl 模式下的服务名称
# 仅当 FRP_ADMIN_FRPS_MANAGER=systemctl 时有效
FRP_ADMIN_FRPS_SERVICE=frps

# ----- frpc 相关配置 -----

# frpc 二进制文件路径（用于验证配置等功能）
# 默认：与 frp-admin 同目录下的 frpc
FRP_ADMIN_FRPC_PATH=/usr/local/bin/frpc

# ----- 安全配置 -----

# CORS 允许的来源，多个用逗号分隔
# 默认为空，表示仅允许同源请求
# 前后端分离开发时需要配置，例如：http://localhost:5173
FRP_ADMIN_CORS_ORIGINS=


# ============================================
# 配置示例
# ============================================

# 示例1：frps 与 frp-admin 在同目录，直接管理进程
# ---------------------------------------------
# FRP_ADMIN_PORT=8080
# FRP_ADMIN_FRPS_MANAGER=process
# （其他使用默认值）

# 示例2：frps 由 systemd 管理
# ---------------------------------------------
# FRP_ADMIN_PORT=8080
# FRP_ADMIN_FRPS_PATH=/usr/local/bin/frps
# FRP_ADMIN_FRPS_CONFIG=/etc/frp/frps.toml
# FRP_ADMIN_FRPS_MANAGER=systemctl
# FRP_ADMIN_FRPS_SERVICE=frps

# 示例3：自定义所有路径
# ---------------------------------------------
# FRP_ADMIN_PORT=9000
# FRP_ADMIN_DB=/var/lib/frp-admin/data.db
# FRP_ADMIN_SECRET=my-super-secret-key
# FRP_ADMIN_FRPS_PATH=/opt/frp/frps
# FRP_ADMIN_FRPS_CONFIG=/opt/frp/frps.toml
# FRP_ADMIN_FRPC_PATH=/opt/frp/frpc
# FRP_ADMIN_FRPS_MANAGER=process
